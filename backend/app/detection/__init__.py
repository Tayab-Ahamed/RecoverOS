"""Deterministic risk detection.

This is a sibling of `agents/`, not a member of it. Detection is arithmetic
over known facts; using a language model here would add cost and
nondeterminism while removing the ability to reproduce a benchmark.
"""
