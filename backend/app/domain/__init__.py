"""Pure domain layer. Depends on the Python standard library ONLY.

This constraint is load-bearing: it is what makes the recovery loop testable
and auditable without a database, a network, or an LLM.
"""
